# RL / simulation environment (Python 3.7)
#
# The language-model planner runs in a separate Python 3.10 environment; see
# requirements-llm.txt. These are the versions the reported results were produced
# with. Transitive dependencies are pinned too, so the environment can be
# reconstructed exactly.
#
# ── PyTorch (CUDA 11.7) ───────────────────────────────────────────────────────
# Install separately BEFORE the rest, so pip resolves the CUDA build:
#   pip install torch==1.13.1+cu117 torchvision==0.14.1+cu117 \
#       --extra-index-url https://download.pytorch.org/whl/cu117
torch==1.13.1+cu117
torchvision==0.14.1+cu117   # not imported directly; required by CityLearn

# ── Simulation framework ──────────────────────────────────────────────────────
# CityLearn supplies the Environment and EpisodeTracker base classes used by
# environment.py and agents.py.
CityLearn==2.1.2
gym==0.26.2
gymnasium==0.28.1

# ── Scientific / data ─────────────────────────────────────────────────────────
numpy==1.21.6
pandas==1.3.5
scipy==1.7.3
scikit-learn==1.0.2         # required by CityLearn
simplejson==3.20.2          # utilities.py

# ── Plotting and the interactive RBC schedule editor ──────────────────────────
matplotlib==3.5.3
ipywidgets==8.1.8           # SolarTrackerRBCEditor in rbc.py

# ── Pinned transitive dependencies ────────────────────────────────────────────
cloudpickle==2.2.1
joblib==1.3.2
threadpoolctl==3.1.0
Pillow==9.5.0
cycler==0.11.0
kiwisolver==1.4.5
fonttools==4.38.0
pyparsing==3.1.4
python-dateutil
six
pytz==2026.2
packaging
typing_extensions==4.7.1
importlib-metadata==6.7.0
zipp==3.15.0
exceptiongroup==1.3.1
cached-property==1.5.2
colorama==0.4.6
tqdm==4.67.3
jupyterlab_widgets==3.0.16
widgetsnbextension==4.0.15

# ── Optional: LM Studio backend ───────────────────────────────────────────────
# llm/client.py exposes QwenClient, which talks to an OpenAI-compatible endpoint
# served by LM Studio. It imports `openai` lazily inside the request method, so
# the package is only needed if you use that backend. Every script here uses
# LocalQwenClient instead, which serves the fine-tuned adapter through
# finetune/serve_inference.py and needs nothing from this block.
#
# openai==1.39.0
# httpx==0.24.1
# httpcore==0.17.3
# pydantic==2.5.3
# pydantic_core==2.14.6
# annotated-types==0.5.0
# anyio==3.7.1
# sniffio==1.3.1
# distro==1.9.0
# h11==0.14.0
# requests==2.31.0
# urllib3==1.26.13
# charset-normalizer==3.4.7
# idna==3.10
# certifi
